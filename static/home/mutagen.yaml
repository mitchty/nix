sync:
  defaults:
    ignore:
      paths:
        - ".zfs"
        - ".direnv"
        - ".#*"
        - "*.desktop"
        - "result"
        - "*.sync-conflict-*"
        - ".DS_Store"
        - ".directory"
        - "*.swp"
        - ".terraform"
      vcs: false
