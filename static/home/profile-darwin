# -*- mode: Shell-script; -*-

# macos specific
alias oe="open -a $(which emacs)"

# mnemonic: Open Nix (macos) App (bundle)
function ona() {
    open -a "/Applications/Nix\ Apps/${$1?need the app name to open}.app"
}
